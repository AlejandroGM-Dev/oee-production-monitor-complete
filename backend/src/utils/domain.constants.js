export const MACHINE_STATES = Object.freeze({
  RUNNING: "RUNNING",
  STOPPED: "STOPPED",
  ALARM: "ALARM",
  MAINTENANCE: "MAINTENANCE",
});

export const MACHINE_EVENT_TYPES = Object.freeze({
  STATE_CHANGE: "STATE_CHANGE",
  ALARM: "ALARM",
  PRODUCTION_COUNT: "PRODUCTION_COUNT",
});

export const DEFAULT_MACHINE_STATE = MACHINE_STATES.STOPPED;